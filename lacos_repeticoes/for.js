const numeros = [100, 200, 300, 400, 500, 600];
                //[0,1,2,3,4,5] indices


for (let i = 0; i < numeros.length; i++){  //let i sera responsavel por nos gerar um numero que usaremos como indice
    console.log(i,numeros[i])
}
